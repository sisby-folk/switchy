{
	"schemaVersion": 1,
	"id": "${cardinalId}",
	"version": "${version}",
	"name": "${cardinalName}",
	"description": "${cardinalDescription}",
	"authors": [
		"${authors}"
	],
	"contributors": [
		"${contributors}"
	],
	"contact": {
		"homepage": "${homepage}",
		"issues": "${issues}",
		"sources": "${sources}"
	},
	"license": "${license}",
	"icon": "assets/${modId}/icon.png",
	"modmenu": {
		"parent": "${modId}"
	},
	"mixins": [
		"${cardinalId}.mixins.json"
	],
	"depends": {
		"minecraft": ">=${mc}",
		"fabricloader": ">=${fl}",
		"fabric-api": ">=${fapi}",
		"${coreId}": ">=${version}"
	},
	"entrypoints": {
		"switchy": [
			"folk.sisby.switchy.SwitchyClientServer"
		],
		"client": [
			"folk.sisby.switchy.client.SwitchyClient"
		]
	}
}
