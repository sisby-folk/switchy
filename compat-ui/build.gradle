dependencies {
	api project(path: ':compat', configuration: 'namedElements')
	api project(path: ':ui', configuration: 'namedElements')
	modImplementation libs.qfapi
	modCompileOnly libs.calio
	modCompileOnly(libs.origins) {
		exclude module: 'apoli'
	}
}
